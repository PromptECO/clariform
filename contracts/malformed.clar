;; Original is valid but with malformed layout and indentation 
(define-read-only (plus 
                  (n int)
       )
  (let (
    (value (+ n 1))
        )              
    value
   )
 )

;; EOF
